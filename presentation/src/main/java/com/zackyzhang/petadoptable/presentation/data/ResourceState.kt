package com.zackyzhang.petadoptable.presentation.data

/**
 * Represents the state in which a [Resource] is currently in
 */
enum class ResourceState {
    LOADING, SUCCESS, ERROR, UPDATE_FINISH, FAVORITE_STATUS
}